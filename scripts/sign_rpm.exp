#!/usr/bin/expect -f

set timeout -1
set passphrase $env(GPG_PASSPHRASE)

# Loop over RPM files passed as arguments
foreach rpm_file $argv {
    spawn rpmsign --addsign $rpm_file
    expect {
        "Enter*" {
            send "$passphrase\r"
            exp_continue
        }
        eof
    }
}